{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hope.local/schemas/polish/record-v1.schema.json",
  "title": "Hope polish record version 1",
  "description": "Structural schema. The shared Polish runtime revalidates the embedded run, derived result, and record digest.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "feature",
    "version",
    "run",
    "result",
    "recordDigest"
  ],
  "properties": {
    "feature": { "const": "polish-record" },
    "version": { "const": 1 },
    "run": { "$ref": "run-v2.schema.json" },
    "result": {
      "type": "object",
      "additionalProperties": false,
      "required": ["changed", "status", "verificationStatus"],
      "properties": {
        "changed": { "type": "boolean" },
        "status": {
          "enum": ["revised", "no-change", "needs-alignment"]
        },
        "verificationStatus": {
          "enum": [
            "verified-in-checked-scope",
            "incomplete",
            "failed",
            "not-completed"
          ]
        }
      }
    },
    "recordDigest": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    }
  }
}
